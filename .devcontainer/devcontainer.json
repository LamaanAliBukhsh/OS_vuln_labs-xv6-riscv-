git commit -m "Fix Codespace setup logging"{
  "name": "xv6 RISC-V Vulnerability Labs",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installZsh": false,
      "upgradePackages": true
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git build-essential gdb-multiarch gcc-riscv64-linux-gnu binutils-riscv64-linux-gnu qemu-system-misc bc && sudo ln -sf /usr/bin/riscv64-linux-gnu-gcc /usr/bin/riscv64-unknown-elf-gcc && sudo ln -sf /usr/bin/riscv64-linux-gnu-ld /usr/bin/riscv64-unknown-elf-ld && sudo ln -sf /usr/bin/riscv64-linux-gnu-objcopy /usr/bin/riscv64-unknown-elf-objcopy && sudo ln -sf /usr/bin/riscv64-linux-gnu-objdump /usr/bin/riscv64-unknown-elf-objdump && sudo ln -sf /usr/bin/riscv64-linux-gnu-as /usr/bin/riscv64-unknown-elf-as && echo 'Setup complete! Run: make qemu'",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.makefile-tools"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },

  "forwardPorts": [],
  
  "remoteUser": "vscode"
}
